import Cocoa

extension AppDelegate {

    func setupEnhancedMenus() {
        // Theme shortcuts are now handled in setupMenuBar() via Theme submenu
        // Focus mode is handled via Cmd+Shift+F in the View menu
        // This method is kept for potential future enhanced menu additions
    }
}