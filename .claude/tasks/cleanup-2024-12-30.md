# Parchment Cleanup Task List
**Created**: 2024-12-30
**Status**: In Progress

## Review Scores
| Category | Score |
|----------|-------|
| Architecture | 67/100 |
| Security | 7.5/10 |
| Performance | 67/100 |
| Code Quality | 5.5/10 |

---

## CRITICAL (Crash/Leak Risk)

### 1. Add deinit to MarkdownViewController
- **File**: `Sources/Parchment/ViewControllers/MarkdownViewController.swift`
- **Issue**: NotificationCenter observers never removed (lines 51-64, 115-119)
- **Fix**: Add deinit with `NotificationCenter.default.removeObserver(self)`

### 2. Add deinit to FileWatcher
- **File**: `Sources/Parchment/Utilities/FileWatcher.swift`
- **Issue**: DispatchSource not cancelled on dealloc
- **Fix**: Add deinit that calls `stop()`

### 3. Fix force unwraps in MainWindowController
- **File**: `Sources/Parchment/Windows/MainWindowController.swift:93-107`
- **Issue**: `findBarView!`, `contentStackView!`, `dropView!` will crash if nil
- **Fix**: Use guard-let or restructure initialization

---

## HIGH (Security/Performance)

### 4. Fix IPv6 SSRF protection
- **File**: `Sources/Parchment/Utilities/ImageLoader.swift:198-220`
- **Issue**: Only blocks IPv4 private IPs, not IPv6 (::1, fe80::, fc00::, fd00::)
- **Fix**: Add IPv6 private range detection

### 5. Fix path traversal vulnerability
- **File**: `Sources/Parchment/App/AppDelegate.swift:28-33`
- **Issue**: String concatenation allows `../` traversal
- **Fix**: Use `URL.standardizedFileURL` to normalize paths

### 6. Fix syntax highlighter cache key
- **File**: `Sources/Parchment/Rendering/CodeSyntaxHighlighter.swift:27`
- **Issue**: Uses `code.prefix(100)` - causes cache collisions
- **Fix**: Use `code.hashValue` for unique keys

### 7. Move parsing off main thread
- **File**: `Sources/Parchment/ViewControllers/MarkdownViewController.swift:161-172`
- **Issue**: Synchronous parsing blocks UI
- **Fix**: Parse in background, render result on main thread

### 8. Delete duplicate ImageCache class
- **File**: `Sources/Parchment/Utilities/ImageLoader.swift:224-248`
- **Issue**: Duplicate of internal cache, wastes 50MB
- **Fix**: Delete entire ImageCache class

### 9. Remove dead image methods
- **File**: `Sources/Parchment/ViewControllers/MarkdownViewController.swift:199-228`
- **Issue**: `createImagePlaceholder`, `scaleImage` unused
- **Fix**: Delete both methods

### 10. Delete unused navigation views
- **Files**:
  - `Sources/Parchment/Navigation/BreadcrumbNavigationView.swift`
  - `Sources/Parchment/Navigation/FloatingTOCView.swift`
- **Issue**: Not integrated into UI
- **Fix**: Delete both files

---

## MEDIUM (Code Quality)

### 11. Add CSP to QuickLook extension
- **File**: `Sources/Parchment/QuickLook/QuickLookExtension.swift:36-167`
- **Issue**: HTML template lacks Content-Security-Policy
- **Fix**: Add CSP meta tag

### 12. Cache compiled regex patterns
- **File**: `Sources/Parchment/Rendering/CodeSyntaxHighlighter.swift:286-298`
- **Issue**: Regex compiled every call (ReDoS risk, slow)
- **Fix**: Static cache for compiled patterns

### 13. Consolidate DocumentMetadata structs
- **Files**:
  - `Sources/Parchment/Models/MarkdownDocument.swift:21-37`
  - `Sources/Parchment/Performance/FastFileOpener.swift:29-35`
  - `Sources/Parchment/Utilities/PerformanceOptimizer.swift:180-196`
- **Issue**: Three nearly identical structs
- **Fix**: Use single shared struct

### 14. Extract word counting logic
- **Issue**: Same word counting code in 4+ files
- **Fix**: Add `String.wordCount` extension

### 15. Consolidate showError methods
- **Files**:
  - `Sources/Parchment/App/AppDelegate.swift:537-543`
  - `Sources/Parchment/Windows/MainWindowController.swift:422-428`
- **Issue**: Identical duplicate code
- **Fix**: Extract to shared utility

### 16. Fix access control
- **File**: `Sources/Parchment/ViewControllers/MarkdownViewController.swift`
- **Issue**: Properties marked `internal` should be `private(set)`
- **Fix**: Update access modifiers

### 17. Add symlink validation
- **Issue**: App follows symlinks without validation
- **Fix**: Check for symlinks before file access

### 18. Block dangerous URL schemes
- **File**: `Sources/Parchment/Rendering/EnhancedMarkdownRenderer.swift:224-225`
- **Issue**: `javascript:` and `data:` URLs not blocked
- **Fix**: Validate URL scheme before setting link

---

## LOW (Polish)

### 19. Remove unused properties
- `currentCursorLine` - never read
- `visibleRange` - updated but never used
- `preloadedParsers` - always empty

### 20. Extract magic numbers
- `50_000` chunk size
- `5.0` cache age
- `0.5` similarity threshold

### 21. Mark classes as final
- `MarkdownDocument`, `DiffHighlighter`, `StyleManager`, etc.

---

## VERIFY

### 22. Build and test
- Run `swift build`
- Test document loading
- Test theme switching
- Test export functionality
