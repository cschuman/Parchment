<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow reading/writing user-selected files -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    
    <!-- Allow reading from Downloads folder -->
    <key>com.apple.security.files.downloads.read-write</key>
    <true/>
    
    <!-- Allow network access for remote images -->
    <key>com.apple.security.network.client</key>
    <true/>
    
    <!-- Don't use app sandbox for now (can enable later for App Store) -->
    <key>com.apple.security.app-sandbox</key>
    <false/>
    
    <!-- Allow incoming Apple Events (for drag & drop) -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>
</dict>
</plist>